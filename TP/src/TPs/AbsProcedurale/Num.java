public class Num {
    public static boolean isPrime(intp) {
        /*
        * @requires : n > 0 and d > 0
        * @return */
        if(p<=1) return false;
        for(int i = 2; i*i<=p;i++){
            if (p%i == 0) return false;
            i++;
        }
    }
}